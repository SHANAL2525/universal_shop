import { Minus, Plus } from 'lucide-react';
export function QuantitySelector({ value, max, disabled, onChange }: { value: number; max: number; disabled: boolean; onChange: (value: number) => void }) { return <div><label className="option-label">Quantity</label><div className="quantity"><button aria-label="Decrease quantity" disabled={disabled || value <= 1} onClick={() => onChange(value - 1)}><Minus/></button><output aria-live="polite">{value}</output><button aria-label="Increase quantity" disabled={disabled || value >= max} onClick={() => onChange(value + 1)}><Plus/></button></div></div>; }
